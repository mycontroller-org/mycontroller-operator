apiVersion: mycontroller.org/v1
kind: MyController
metadata:
  name: myc-with-pvc
spec:
  secret: my-secret
  disable_analytics: true
  log_level: info
  storage:
    storage_class_name: ""
    data_size: "10Gi"
    metric_size: "10Gi"

